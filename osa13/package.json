{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "NODE_ENV=production npm start --prefix backend",
    "test": "TESTING=true node --test --test-concurrency=1",
    "start:test": "TESTING=true npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "axios": "^1.20.0"
  }
}
